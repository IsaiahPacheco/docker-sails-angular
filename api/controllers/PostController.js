/**
 * BlogController
 *
 * @description :: Server-side logic for managing posts
 * @help        :: See http://links.sailsjs.org/docs/controllers
 */

module.exports = {
	// findBySlug: function (req, res) {
	// 	var slug = req.param("slug");

	// 	Post.findOneBySlug(slug).exec(function(err, found) {
	// 		if(found) {
	// 			res.send(found);
	// 		} else {
	// 			res.status(404).send('Post not found, check your url');
	// 		}
	// 	});
	// }
};

